# 1) Check NODE_ENV (optional)
echo $env:NODE_ENV

# 2) If it’s set to something weird, remove it (recommended)
Remove-Item Env:NODE_ENV -ErrorAction SilentlyContinue

# 3) Clean + build
Remove-Item -Recurse -Force .next
npm run build

# 4) Start
node .next\standalone\server.js

cloudflared tunnel run comfyui-otg2

