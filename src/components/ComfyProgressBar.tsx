"use client";

/**
 * ComfyProgressBar (disabled)
 * --------------------------
 * You asked for a clean loading indicator (no progress bar). This component is kept
 * as a harmless placeholder so imports won't break builds.
 *
 * If you later want a real progress bar, we can re-introduce a progress store.
 */
export function ComfyProgressBar() {
  return null;
}
